````C
{
	//中、英、德、法、意、西班牙、日本、波兰、瑞典0、韩语、匈牙利0、希腊0、阿拉伯0、捷克
}

{
	1、电池，usb上电不开机；2、优化UI；3、修改默认参数；4、优化屏和摄像头效果；
}

{  
	1，VGA图像变形，实际成像分辨率横向可以坐下调整（已完成）
	2，VGA拍照的关联屏上面显示的图标是问号（已完成）
	3，图像颜色没问题还是重点调下屏显就好了，皮肤也看了还是屏显的问题
	4，低电3.35V还显示一格电量，应该要显示红色然后关机（已完成）
	5，未插卡提示框跟拔电关机提示框的中间有一条竖线（已完成）
	6，语言默认英文（已完成）
}

{
	SDK：hx35xx_camera_sdk_v0.0.7_20250822
	莫工，ADKEY有办法通过打印电压，逆推电阻的的阻值吗，有计算公式吗，
	上拉电阻是100K
}

{
	1、修改四路分屏预览；2、优化屏和摄像头效果；3、修改默认参数；4、修改库（TE功能，去掉打印）；
}

{
	SDK：hx35xx_car_sdk_v0.0.11_20250926
	屏IC：S6E63J0(OLED屏，无背光IO)
	OLED屏无背光IO，不能通过IO口控制背光息屏，
	客户需要息屏的功能
	请教下是否通过写寄存器进入睡眠模式，实现息屏
	mipi屏是否需要通过hx350x_MipiDsiCmdTx或者hx350x_MipiDsiParaTx写寄存器命令
	杰里驱动、datasheet，SDK TE改切线问题，同一SDK，同一屏
}

{
	0000 0101 0x05
	0000 0111 0x07

	0001 0101 0x15
	
	0011 0101 0x35
	0011 0111 0x37
	0011 1001 0x39
}

{
	3400 = R * 4096 / (R + 100)
	3400 * (R + 100) = R * 4096
	0.8 * R + 80 = R
	0.2 * R = 80
	R = 400
	
	3400 = 400 * 4096 / 500
	
	value / 4096 = R / (R + 100)
	value / 4096 *(R + 100) = R
	(value / 4096) * R + (value / 4096) * 100 = R
	(1 - (value / 4096)) * R = (value / 4096) * 100
	R = ((value / 4096) * 100) / (1- (value / 4096))
	R = ((3199 / 4096) * 100) / (1 - (3199 / 4096))
	
	M :
	3199 : 356.6
	3584 : 399.5
	380
	
	D :
	
}

{
	SDK：HX330x_SDK_2M_release_v0.457_20241203
	我们有客户希望通过摄像头，检测颜色，如血液
	有没有接口可以实现
}

{
	1、生成uv数据图像，暂时还没有接口可以直接调用是吗？
	2、调用ISP是希望筛选出红色的像素对吗？
	3、uv数据图像中，像素有颜色范围吗？可以读出某个像素的16进制颜色数据吗
}

{
	我们SDK可以提供摄像头的数据（YUV格式），
	原厂提供了个解决方案，
	可以隔几秒生成一个小尺寸的图像，通过ISP筛选掉非红像素，然后再遍历非黑像素，
	通过采样，可以先确定所需红色的y值范围，然后遍历所有非黑像像素，
	计算非黑像素中在y值范围内的像素数量，
	这个像素数量大于某个值时，判断出现了红色（血液）
}

{
	1、调整摄像头预览顺序；2、添加多国语言；3、优化S6E63J0;4、添加息屏亮屏功能；
}

{
	1，3录时镜头顺序没改过来还是反的（已完成）
	2，今天查看相片回放界面还有停车监控图标，夜视灯还在中间位置-图1（已完成）
	3，按按键的时候画面会卡一下
	4，按上键切换镜头顺序应该为，前录-测录-测录-后路（现在是后录-测录-测录-前录）（已完成）
	5，分辨率默认2K，菜单要有2K-1080P-720P（现在2K-FHD-HD）（已完成）
	6，读卡时蓝灯闪烁，开机不读卡时蓝灯常亮（已完成）
	7，白车中性开机画面（已完成）
	8，四录时开机有声音（3录是开机没声音）（已完成）
	9，默认3分钟自动息屏（已完成）
}

{
	SDK：hx35xx_car_sdk_v0.0.11_20250926
	还是昨天同一个屏，同一个项目，
	现在测试，按按键时，预览卡顿，录像视频不会，
	现在怀疑还是屏卡顿，
	机器和屏都还在原厂，能不能帮忙确认一下，
}

{
	1、调整按键切换摄像头顺序；2、修改UI；3、添加串口功能和录像灯闪烁功能；4、修改默认配置；
}

{
	SDK：hx35xx_car_sdk_v0.0.11_20250926
	同一个项目，录像老化时，预览卡死（测试是大概35分钟），
	按键，菜单功能正常，UI显示（录像走时，图标切换正常）
	切换后（测试是切换拍照界面）恢复预览画面，
	打印如下，视频如下
}

{
	1、优化开机声音；2、优化息屏流程；3、优化摄像头预览显示；4、降低移动检测敏感度；
}

{
	1、优化屏亮度；2、优化UI；3、设置插电脑进入菜单；4、修改vga分辨率；
}

{
	50
	4
	3：卡在开机画面，进入摄像头慢
	1：卡死
}

{
	还是这个问题，老化了50台，有一台出现卡死
	看看能不能把bta时序加到10
}

{
	这个项目，50台中（bta + 5），
	有3台概率卡死在开机画面，概率开机画面时间过长
	我这边 +8，我这里的机器也卡死在卡机画面了
}
````