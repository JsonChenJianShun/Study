````C
{
	VDDCORE:提供电压的电源电路；
	LDO:成本低，转换效率低，噪声低；
	DCDC:成本高，转换效率高，噪声高；
}

{
	“Qt”本身发音像英语单词Cute（可爱），在中文里常直接读其英文字母 Q-T。它最核心的含义是指一个功能强大的跨平台C++应用程序开发框架。

简单来说，它提供了一套统一的工具和代码库，让开发者能用同一份源代码，快速开发出能运行在Windows、macOS、Linux、Android、iOS甚至嵌入式系统上的软件。
}

{
	SRAM（Static Random-Access Memory，静态随机存取存储器）：快，断电易丢失；
}

{
	电脑对机器供电：使用USB 3.0（900 mA）或者有源USB hub
}

{
	在嵌入式系统中，主控扩频（Spread Spectrum Clocking，SSC） 是一种通过调制时钟频率来降低电磁干扰（EMI）的技术。
}

{
	模运算：
	// 临时转换为0-3计算，再转回1-4
	int temp = rect_x_flag - 1;  // 1-4转0-3

	if (flag)
		temp = (temp + 1) % 4;   // 递增
	else  
		temp = (temp - 1 + 4) % 4;   // 递减

	rect_x_flag = temp + 1;      // 0-3转回1-4
}

{
	AVora 1.2.1可以做内窥镜的方案吗?
	现在我们用这套SDK，客户的USB的摄像头没有点亮；
}

{
	【项目代号】：X5005
	【客户需求】：带wifi内窥镜
	【软硬环境】：AVora 1.2.1 3506F主控
	【应用市场】：内窥镜
	【问题描述】：客户提供的USB摄像头没有点亮
}

{
	xy:100:180
	wh:298x606
	
	1920:1080
	1280:720
	
	
	1280:320 = 4
	720:240 = 3
	
	1704:960
	1704:320 = 5.325
	960:240 = 4
	
	300 / 5.325 = 56
	600 / 4 = 150
	
	100 / 5.325 = 19
	180 / 4 = 45
}

{
	//relax,how to relax,wait,don't move,happy,relax,I am right,don't scale,wait wait,relax,I can go,no scale,you are best,come on,baby,you are the best,cheer up,cheer up,people move to life,cheer up;happy，happy,wait,wait;relax,relax,hahahahha,happy,life is beatiful,happy,so happy,so happy,no no no,oh oh oh,cool cool cool ,brstiful, cool cool cool ,beatiful beatiful beatiful,no scale,all is beatiful,cool;happy,happy,cool,cool,cool cool happy happy,can't understand,can't understand;happy happy,cool cool,let's crazy crazy happy happy,cheer up cheer up,you are the best,life is beatiful;cheer up cheer up,crazy crazy,happy happy;I guess he don't know,happy happy 
}

{
	屏分辨率是1280x320
	用3506F AVora 1.2.1
	3录和4录能不能实现下图的效果
}

{
	bmp：
	分辨率：60x60；
	位深：8；
	背景：0x808080
	
	JPG:
	分辨率：1280x720
}

{
	AVora 1.2.0 app预览相对机器有延迟，看看能不能优化；
	或者1.2.1有没有这方面的优化
}

{
	打印：PF10,
	powerkey:PD8
}

{
	8405，
	N88,W668,
}

{
	AVora 1.2.0
	1、do_sleep_mode()和do_power_off()现象不一样，需要兼容ACC和停车唤醒；
	2、使用1920x1080p屏幕（video1280x720放大），vga预览异常，对比发现，摄像头分辨率高不能小于屏video分辨率高（其他项目裁剪时发现）,不带wifi已经解决；
}

{
	AVora:
	{
		84A3，
		8405，
		N88,
		w668,
	}
}

{
	Analog-to-Digital Converter；
	模数转换器；
}


{
	dark reader:浏览器网页全黑；
	向日葵：最多可授权10台；
	联合体{
		1. 联合体特性
		union 的所有成员共享同一块内存
		总大小等于最大成员的大小（这里两个 struct 都是 8 字节）
		可以通过不同方式访问相同的数据
		{
			struct {
				u16 x;        // 左上角 x 坐标（16位）
				u16 y;        // 左上角 y 坐标（16位）
				u16 height;   // 矩形高度（16位）
				u16 width;    // 矩形宽度（16位）
			};
			内存布局：[x][y][height][width]（各占2字节，共8字节）
			struct {
				u32 coor;     // 坐标打包：高16位=x，低16位=y
				u32 size;     // 尺寸打包：高16位=height，低16位=width
			};
			内存布局：[coor(32位)][size(32位)]（各占4字节，共8字节）
		}
	}

	strcat{
		#include <stdio.h>
		#include <string.h>

		int main() {
			char str1[20] = "Hello";  // 必须确保有足够空间
			char str2[] = " World!";
			
			strcat(str1, str2);  // 将 str2 连接到 str1 后面
			
			printf("%s\n", str1);  // 输出: Hello World!
			
			return 0;
		}
	}

	带宽：带宽决定了你云服务器的“高速公路”有多宽，它直接影响服务器与外界的网络传输速度和流量上限。
}


````